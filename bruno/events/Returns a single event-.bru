meta {
  name: Returns a single event-
  type: http
  seq: 3
}

get {
  url: https:/commlink.digitaldarkness.com/api//events/:event_id
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}
