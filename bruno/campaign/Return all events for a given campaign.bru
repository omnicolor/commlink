meta {
  name: Return all events for a given campaign
  type: http
  seq: 5
}

get {
  url: https:/commlink.digitaldarkness.com/api//campaigns/:campaign_id/events
  body: none
  auth: bearer
}

params:query {
  ~future: 
}

auth:bearer {
  token: {{token}}
}
