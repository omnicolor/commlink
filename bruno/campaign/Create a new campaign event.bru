meta {
  name: Create a new campaign event
  type: http
  seq: 6
}

post {
  url: https:/commlink.digitaldarkness.com/api//campaigns/:campaign_id/events
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}
