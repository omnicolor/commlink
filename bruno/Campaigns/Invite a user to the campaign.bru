meta {
  name: Invite a user to the campaign
  type: http
  seq: 7
}

post {
  url: https:/commlink.digitaldarkness.com/api//campaigns/:campaign_id/invite
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}
