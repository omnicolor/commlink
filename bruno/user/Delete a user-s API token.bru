meta {
  name: Delete a user-s API token
  type: http
  seq: 4
}

delete {
  url: {{host}}/users/{{user_id}}/token/:token_id
  body: none
  auth: bearer
}

params:path {
  token_id: 
}

auth:bearer {
  token: {{token}}
}
