meta {
  name: Create a new API token
  type: http
  seq: 3
}

post {
  url: {{host}}/users/{{user_id}}/token
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "expires_at": "",
    "name": ""
  }
}
